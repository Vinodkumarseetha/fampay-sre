apiVersion: v1
kind: ConfigMap
metadata:
  name: hodor-config
data:
  APP_ENV: "production"
  APP_VERSION: "1.0.0"
---
# NOTE: In production use External Secrets Operator or Sealed Secrets.
# This is a placeholder showing structure; never commit real secrets to git.
apiVersion: v1
kind: Secret
metadata:
  name: hodor-secrets
type: Opaque
stringData:
  secret_key: "REPLACE_WITH_REAL_SECRET_VIA_CI"
