apiVersion: v1
kind: Service
metadata:
  name: hodor-service
  labels:
    app: hodor
spec:
  selector:
    app: hodor
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP   # internal only; exposed via ingress under /hodor/
